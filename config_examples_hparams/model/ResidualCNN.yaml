name: ResidualCNN

params:
  c_in: 1 # ${dataset.num_dim}
  c_out: 1 # ${dataset.n_classes} or 1 if binary
  coord: False
  separable: False
  zero_norm: False

hyperparameters_vary:
  c_in:
    optuna_type: const
    value: 1
  c_out:
    optuna_type: const
    value: 1

attack_train_mode: False
