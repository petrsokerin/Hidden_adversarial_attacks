name: S4

params:
  input_dim: 1 # ${dataset.num_dim} of multivariate
  hidden_dim: 128
  output_dim: 1 # ${dataset.n_classes} or 1 if binary
  dropout: 0.1

hyperparameters_vary:
  dropout:
    optuna_type: float
    low: 0.1
    high: 0.7
    step: 0.1

attack_train_mode: False
